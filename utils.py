import os, shutil

def move_file(file_path, destination_dir):
    """
    Moves a file to a destination directory. Creates the directory if it doesn't exist.

    Args:
        file_path (str): The path to the file.
        destination_dir (str): Where the file should be moved.
    """

def organize_by_ext():
    """Explain"""
    